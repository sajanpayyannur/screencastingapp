.well
  %h2= @user.name
  = link_to "← Back to User", admin_user_path(@user), :class => 'btn'

%br
%br

%h4 Message
= simple_form_for [:admin, @user, @message] do |f|
  = f.input :subject
  = f.input :body, :input_html => {:class => "span6", :rows => 10}
  = f.submit
